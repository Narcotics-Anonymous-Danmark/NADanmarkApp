name: adapter_storage
description: KeyValueStorePort adapter on shared_preferences.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  na_kernel:
    path: ../../core/na_kernel
  na_ports:
    path: ../../core/na_ports
  riverpod: 3.3.2
  shared_preferences: 2.5.5

dev_dependencies:
  custom_lint: 0.8.1
  flutter_test:
    sdk: flutter
  na_lints:
    path: ../../../tool/na_lints
  shared_preferences_platform_interface: 2.4.2
