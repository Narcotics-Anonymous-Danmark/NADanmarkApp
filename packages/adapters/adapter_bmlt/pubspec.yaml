name: adapter_bmlt
description: MeetingSearchPort and MeetingFormatsPort adapters on the BMLT JSON API.
publish_to: none
version: 0.0.0

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  dio: 5.11.1
  na_kernel:
    path: ../../core/na_kernel
  na_ports:
    path: ../../core/na_ports
  riverpod: 3.3.2

dev_dependencies:
  custom_lint: 0.8.1
  flutter_test:
    sdk: flutter
  na_lints:
    path: ../../../tool/na_lints
  na_testing:
    path: ../../core/na_testing
