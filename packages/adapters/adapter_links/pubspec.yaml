name: adapter_links
description: ExternalLinksPort adapter on url_launcher.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  na_ports:
    path: ../../core/na_ports
  riverpod: ^3.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  custom_lint: ^0.8.0
  flutter_test:
    sdk: flutter
  na_lints:
    path: ../../../tool/na_lints
  plugin_platform_interface: ^2.1.8
  url_launcher_platform_interface: ^2.3.2
