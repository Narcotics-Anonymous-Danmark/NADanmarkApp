name: adapter_clock
description: System clock, ticker and scheduler adapters on dart:async.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  na_kernel:
    path: ../../core/na_kernel
  na_ports:
    path: ../../core/na_ports
  riverpod: ^3.0.0

dev_dependencies:
  custom_lint: ^0.8.0
  fake_async: ^1.3.0
  na_lints:
    path: ../../../tool/na_lints
  test: ^1.25.0
