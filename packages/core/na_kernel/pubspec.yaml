name: na_kernel
description: Value types, sealed results, time ports and domain events shared by every NA Danmark package.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0
resolution: workspace

dependencies:
  collection: ^1.19.0
  meta: ^1.16.0

dev_dependencies:
  custom_lint: ^0.8.0
  na_lints:
    path: ../../../tool/na_lints
  test: ^1.25.0
