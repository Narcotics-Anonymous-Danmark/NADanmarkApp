import 'package:flutter/widgets.dart';

final class Counter extends StatelessWidget {
  const Counter({super.key});

  @override
  Widget build(final BuildContext context) => const SizedBox();
}

final class State {
  const State();
}

final class Derived extends State {
  const Derived();
}
